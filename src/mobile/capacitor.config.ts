import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.duocuc.duocpoint',
  appName: 'DuocPoint',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
